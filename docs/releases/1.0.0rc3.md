# datefinder 1.0.0rc3

Release date: 2026-03-23

## Highlights

- Supersedes `1.0.0rc2` as the active release candidate for validation.
- Keeps all parser/API changes introduced in `rc2`.
- Adds release-pipeline hardening for artifact publish consistency.

## Packaging and Distribution Updates

- Publish workflows now filter unsupported `pp*` wheels from the distribution bundle before upload.
- Checksum verification is aligned with the filtered publish set.
- Release-candidate workflow references are updated to `1.0.0rc3`.

## Parser Scope

This candidate includes the v2 compatibility updates shipped in `rc2`, including:

- default month-only parsing with opt-out (`allow_month_only=False` / `--no-month-only`)
- opt-in compact numeric parsing (`allow_compact_numeric=True` / `--compact-numeric`)
- multiline matching control (`allow_multiline=False` / `--no-multiline`)

## Validation Summary

- Local tests: `92 passed`.
- Cross-platform artifact build matrix completed before publish promotion.
